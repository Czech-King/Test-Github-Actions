name: Output Demo

on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Create Version
        id: step1
        run: echo "version=1.0.0" >> $GITHUB_OUTPUT

      - name: Use Version
        run: echo "${{ steps.step1.outputs.version }}"
